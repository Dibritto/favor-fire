# Configurações para gerenciar e configurar um backend do Firebase App Hosting.
# https://firebase.google.com/docs/app-hosting/configure-pt-br

runConfig:
  # Aumente este valor se desejar ativar automaticamente
  # mais instâncias em resposta ao aumento do tráfego.
  maxInstances: 1
